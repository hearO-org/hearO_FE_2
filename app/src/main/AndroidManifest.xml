<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 📌 인터넷 (이미 있음) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 📌 마이크 권한(필수) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- 📌 ForegroundService 관련 권한 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- ⚠️ Android 13(API 33)+ 은 POST_NOTIFICATIONS 권한도 필요할 수 있음 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".AppGlobals"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hearo2"
        tools:targetApi="31">

        <!-- ⭐ Splash -->
        <activity
            android:name=".login.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Hearo2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ⭐ 카카오 로그인 리다이렉트 처리 -->
        <activity
            android:name=".login.LoginRedirectActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="hearo"
                    android:host="login" />
            </intent-filter>
        </activity>

        <!-- 로그인 관련 -->
        <activity android:name=".login.LoginActivity" android:exported="true" />
        <activity android:name=".login.LoginEmailActivity" android:exported="true" />

        <!-- 온보딩 단계 -->
        <activity android:name=".signup.SignUpInfoActivity" android:exported="true" />
        <activity android:name=".signup.SignUpTermsActivity" android:exported="true" />
        <activity android:name=".signup.SignUpProfileActivity" android:exported="true" />
        <activity android:name=".signup.SignUpKeywordActivity" android:exported="true" />
        <activity android:name=".signup.SignUpCompleteActivity" android:exported="true" />

        <!-- 메인화면 -->
        <activity android:name=".MainActivity" android:exported="true" />

        <!-- ⭐⭐⭐ 소리 감지 ForegroundService 등록(가장 중요) -->
        <service
            android:name=".sound.SoundDetectionService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

    </application>

</manifest>
